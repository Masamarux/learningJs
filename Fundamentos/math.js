const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);//um função q calcula o pi e outra q calcula a potência

console.log(area);
console.log(typeof Math);//math é um objeto do javascript diferente do Number q é uma função
