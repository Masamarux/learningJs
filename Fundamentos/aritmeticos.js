const [a, b, c, d] = [3, 5, 1 ,15];

//operadores binários
const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

//existem operadores unários como - colocado na frente de uma variavel tornando ela negativa, no caso da divisao

console.log(soma, subtracao, multiplicacao, -divisao, modulo);